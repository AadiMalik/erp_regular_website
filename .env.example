VITE_API_BASE_URL=https://erp.example.com/api

# This storefront's business in the ERP - drives which business-wise
# Website Theme settings are fetched and applied on load (see
# src/services/websiteTheme.js). Set by the business/deployment, never by
# end users.
VITE_BUSINESS_ID=
